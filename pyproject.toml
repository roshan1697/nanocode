[project]
name = "nanocode"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Roshan", email = "roshanpoojary16@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "ollama>=0.6.2",
    "openai>=3.8.0",
    "requests>=2.34.2",
]

[project.scripts]
nanocode = "nanocode:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
